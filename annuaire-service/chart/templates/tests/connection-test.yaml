---
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "annuaire.fullname" . }}-test"
  labels:
    {{- include "annuaire.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36
      command: ["wget"]
      args:
        - "-qO-"
        - "http://{{ include "annuaire.fullname" . }}:{{ .Values.service.port }}/healthz"
